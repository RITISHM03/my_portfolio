document.getElementById("contact-form").addEventListener("submit", function(event) {
    event.preventDefault();
    // Code to handle form submission (you can use AJAX to send form data to a server)
    alert("Form submitted!");
});
